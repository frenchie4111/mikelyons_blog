<% provide(:back, "/admin/categories") %>
<%= form_for( @category, url:admin_category_path(@category), :remote => true) do |f| %>
    <%= f.label :name %>: <%= f.text_field :name, content: @category.name, style: "width: 500px;" %>

    <br/>
    <%= f.submit %><a id="update_text"></a>
<% end %>